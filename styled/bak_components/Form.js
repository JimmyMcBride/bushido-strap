import styled from "styled-components";
import { flexColumnBase, sizeBase, styleBase } from "../base/index";

const Form = styled.form`
  ${flexColumnBase}
  ${sizeBase}
  ${styleBase}
`;

export default Form;
