import styled from "styled-components";
import {
  sizeBase,
  styleBase,
  imageBase,
  flexRowBase,
  shapeBase,
} from "../base/index";

const Flex = styled.div`
  ${flexRowBase}
  ${sizeBase}
  ${styleBase}
  ${imageBase}
  ${shapeBase}
`;

export default Flex;
