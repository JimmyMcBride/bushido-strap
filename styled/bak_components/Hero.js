import styled from "styled-components";
import {
  heroBackgroundBase,
  sizeBase,
  heroStyleBase,
  flexColumnBase,
} from "../base/index";

const Hero = styled.div`
  ${flexColumnBase}
  ${heroBackgroundBase}
  ${heroStyleBase}
  ${sizeBase}
`;

export default Hero;
