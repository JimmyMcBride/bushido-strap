import styledMap from "styled-map";

export default styledMap`
  size_len: length;
  size_cover: cover;
  size_contain: contain;
  size_initial: initial;
  size_inherit: inherit;
  size_auto: auto;
  default: cover;
`;
