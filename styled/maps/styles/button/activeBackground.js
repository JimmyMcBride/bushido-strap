import styledMap from "styled-map";
import { theme } from "../../../theme";

export default styledMap`
  red: ${theme.red8};
  orange: ${theme.orange8};
  amber: ${theme.amber8};
  yellow: ${theme.yellow8};
  brown: ${theme.brown8};
  lime: ${theme.lime8};
  green: ${theme.green8};
  teal: ${theme.teal8};
  cyan: ${theme.cyan8};
  skyBlue: ${theme.skyBlue8};
  blue: ${theme.blue8};
  indigo: ${theme.indigo8};
  violet: ${theme.violet8};
  purple: ${theme.purple8};
  pink: ${theme.pink8};
  invert: ${theme.gray8};
  default: ${theme.gray8};
`;
