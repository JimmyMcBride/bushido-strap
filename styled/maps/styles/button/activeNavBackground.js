import styledMap from "styled-map";
import { theme } from "../../../theme";

export default styledMap`
  red: ${theme.red4};
  orange: ${theme.orange4};
  amber: ${theme.amber4};
  yellow: ${theme.yellow4};
  brown: ${theme.brown4};
  lime: ${theme.lime4};
  green: ${theme.green4};
  teal: ${theme.teal4};
  cyan: ${theme.cyan4};
  skyBlue: ${theme.skyBlue4};
  blue: ${theme.blue4};
  indigo: ${theme.indigo4};
  violet: ${theme.violet4};
  purple: ${theme.purple4};
  pink: ${theme.pink4};
  invert: ${theme.gray6};
  default: ${theme.gray4};
`;
