import styled from "styled-components";
import { sizeBase, styleBase, imageBase, shapeBase, fontBase } from "../base";

const Text = styled.div`
  ${sizeBase}
  ${styleBase}
  ${imageBase}
  ${shapeBase}
  ${fontBase}
`;

export default Text;
