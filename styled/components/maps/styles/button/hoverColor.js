import styledMap from "styled-map";
import theme from "../../../../theme";

export default styledMap`
  primary: ${theme.gray0};
  secondary: ${theme.gray0};
  accent: ${theme.gray0};
  success: ${theme.gray0};
  warning: ${theme.gray0};
  danger: ${theme.gray0};
  invert: ${theme.gray1};
  default: ${theme.gray8};
`;
