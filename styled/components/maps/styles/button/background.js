import styledMap from "styled-map";
import theme from "../../../../theme";

export default styledMap`
  primary: ${theme.blue3};
  secondary: ${theme.purple3};
  accent: ${theme.magenta3};
  success: ${theme.green3};
  warning: ${theme.orange3};
  danger: ${theme.red3};
  invert: ${theme.gray1};
  default: ${theme.gray8};
`;
