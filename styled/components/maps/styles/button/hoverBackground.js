import styledMap from "styled-map";
import theme from "../../../../theme";

export default styledMap`
  primary: ${theme.blue4}
  secondary: ${theme.purple4}
  accent: ${theme.magenta4}
  success: ${theme.green4}
  warning: ${theme.orange4}
  danger: ${theme.red4}
  invert: ${theme.gray7};
  default: ${theme.gray2};
`;
