import styled from "styled-components";
import {
  flexColumnBase,
  sizeBase,
  cardStyleBase,
  centerFontBase,
} from "../base/index";

const Card = styled.div`
  ${flexColumnBase}
  ${sizeBase}
  ${cardStyleBase}
  ${centerFontBase}
`;

export default Card;
