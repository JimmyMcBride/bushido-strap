import styled from "styled-components";
import {
  heroBackgroundBase,
  sizeBase,
  heroStyleBase,
  flexColumnBase,
} from "../base";

const Hero = styled.div`
  ${flexColumnBase}
  ${heroBackgroundBase}
  ${heroStyleBase}
  ${sizeBase}
`;

export default Hero;
