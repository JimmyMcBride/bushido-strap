import styled from "styled-components";
import {
  flexColumnBase,
  buttonSizeBase,
  buttonStyleBase,
  centerFontBase,
} from "../base";

const Button = styled.button`
  ${flexColumnBase}
  ${buttonSizeBase}
  ${buttonStyleBase}
  ${centerFontBase}
`;

export default Button;
