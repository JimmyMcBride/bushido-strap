import styled from "styled-components";
import { flexColumnBase, wrapperSizeBase } from "../base/index";

const Wrapper = styled.div`
  ${flexColumnBase}
  ${wrapperSizeBase}
`;

export default Wrapper;
