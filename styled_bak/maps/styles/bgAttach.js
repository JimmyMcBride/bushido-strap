import styledMap from "styled-map";

export default styledMap`
  attachFix: fixed;
  attachLocal: local;
  attachInherit: inherit;
  attachInitial: initial;
  attachScroll: scroll;
  default: scroll;
`;
